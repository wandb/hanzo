# Load build env from shared config (works across git worktrees),
# falling back to a local .env.build for contributors who prefer that.
dotenv_if_exists "$HOME/.config/hanzo/.env.build"
dotenv_if_exists .env.build
